logging:
  level:
    org.springframework: DEBUG
    org.springframework.beans: TRACE
    org.springframework.context: TRACE
    com.raon: DEBUG  # 사용자 코드 로그 (필요 시)
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
